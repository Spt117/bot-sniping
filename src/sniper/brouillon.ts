// import { ethers } from "ethers";

// let contract = new ethers.Contract(contractAddress, contractAbi, provider);

// let result = await contract.callStatic.myFunction(args, {
//     from: myAddress,
//     gasPrice: gasPrice,
//     value: value,
// });

// console.log(`Simulation result: ${result}`);

// let contract2 = new ethers.Contract(contractAddress, contractAbi, signer);

// let estimate = await contract2.estimateGas.myFunction(args, {
//     gasPrice: gasPrice,
//     value: value
// });

// console.log(`Estimated gas: ${estimate}`);
